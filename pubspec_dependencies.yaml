# Copie ces dépendances dans ton pubspec.yaml

dependencies:
  flutter:
    sdk: flutter
  
  # Supabase (auth + cloud database)
  supabase_flutter: ^2.5.0
  
  # SQLite local (offline storage)
  drift: ^2.16.0
  sqlite3_flutter_libs: ^0.5.20
  path_provider: ^2.1.2
  path: ^1.9.0
  
  # State management
  provider: ^6.1.2
  
  # UI
  google_fonts: ^6.2.1
  fl_chart: ^0.66.2  # Pour graphiques stats
  
  # Utils
  intl: ^0.19.0
  uuid: ^4.3.3
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  drift_dev: ^2.16.0
  build_runner: ^2.4.8
  flutter_lints: ^3.0.1
